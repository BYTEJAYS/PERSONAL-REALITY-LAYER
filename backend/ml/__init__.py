"""Lightweight, dependency-free ML for PRL.

A from-scratch logistic-regression predictor — real fitted weights, trained by
gradient descent, evaluated on held-out data. Pure Python so it runs anywhere
(no numpy/sklearn) and stays fully interpretable: the coefficients are the
feature importances.
"""
