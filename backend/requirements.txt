fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3
pgvector==0.3.6
neo4j==5.27.0
redis==5.2.1
python-dateutil==2.9.0.post0
httpx==0.28.1
# Real semantic embeddings (local, offline). torch is installed CPU-only in the
# Dockerfile to keep the image lean — don't add a GPU torch pin here.
sentence-transformers==3.3.1
